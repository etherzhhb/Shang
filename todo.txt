Change CTV to VTM.

Support Immediate number operands.

Support memory operation

-view-dag-combine1-dags -view-isel-dags -march=verilog $(TargetDir)a.ll -vbe-res-config-script=$(TargetDir)Configure.lua -vbe-design-name=dut -o $(TargetDir)a.v -disable-fdms

Overall, there is no way to define or match SelectionDAG nodes that define multiple values (e.g. SMUL_LOHI, LOAD, CALL, etc). This is the biggest reason that you currently still have to write custom C++ code for your instruction selector.
